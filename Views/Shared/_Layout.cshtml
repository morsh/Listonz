<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8" />
        <title>@ViewBag.Title - My ASP.NET MVC Application</title>
        <link href="~/logo-16x16.jpg" rel="shortcut icon" type="image/x-icon" />
        <meta name="viewport" content="width=device-width" />

        @RenderSection("DefaultStyles", false)

        @Styles.Render("~/Content/css")
        @Scripts.Render("~/bundles/modernizr")

        @RenderSection("CustomStyles", false)
    </head>
    <body>
        <header>
            <div class="content-wrapper">
                <div class="float-left">
                    <span class="site-title">@Html.ActionImage("Index", "Home", null, "~/Images/logo-64x43.jpg", "Home Page")</span>
                    <span>@RenderSection("title", required: false)</span>
                </div>
                <div class="float-right">
                    <section id="login">
                        @Html.Partial("_LoginPartial")
                    </section>
                    <nav>
                        @Html.Partial("_HomeNavigationPartial")
                    </nav>
                </div>
            </div>
        </header>
        <div id="body">
            @RenderSection("featured", required: false)
            <section class="content-wrapper main-content clear-fix">
                @RenderBody()
            </section>
        </div>
        <footer>
            <div class="content-wrapper">
                <div class="float-left">
                    <p>&copy; @DateTime.Now.Year - Listonz</p>
                </div>
            </div>
        </footer>

        @Scripts.Render("~/bundles/jquery")
        @Scripts.Render("~/bundles/ko")
        @Scripts.Render("~/bundles/lz")
        @RenderSection("scripts", required: false)
    </body>
</html>
